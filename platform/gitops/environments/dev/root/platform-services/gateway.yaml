apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: terra-dev-gateway
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: terra
    terra/environment: dev
  annotations:
    argocd.argoproj.io/sync-wave: "40"
spec:
  project: terra-dev-platform
  source:
    repoURL: https://github.com/martindjerf/infra-test-2.git
    targetRevision: main
    path: platform/gitops/platform-services/gateway/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: gateway-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false